[workspace]
resolver = "2"

members = [
  "atlas-layout",
  "atlas-layout-wasm",
]

# The wasm artifact ships to browsers; trade compile time for size and speed.
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
